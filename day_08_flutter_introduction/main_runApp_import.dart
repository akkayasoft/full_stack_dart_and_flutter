import 'package:flutter/material.dart';

class MainRunAppImport extends StatelessWidget {
  const MainRunAppImport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
